name: TEMP Apply Provisional Class Enrollment

on:
  push:
    branches:
      - feat/issue-1011-provisional-email-students

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[skip class-helper-1011]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/issue-1011-provisional-email-students
          fetch-depth: 0
      - name: Apply helper
        run: |
          python .github/tmp_patch_1011_class.py
          git diff --check
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/backend/scripts/student-class-enrollment-import.ts apps/backend/scripts/student-class-enrollment-import.test.ts apps/backend/scripts/student-class-enrollment-import.README.md
          git commit -m "feat(student): enroll provisional students by institutional email (#1011) [skip class-helper-1011]"
          git push origin HEAD:feat/issue-1011-provisional-email-students
