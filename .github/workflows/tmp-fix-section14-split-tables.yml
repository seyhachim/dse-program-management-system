name: One-shot Section 14 split-table fix
on:
  push:
    branches:
      - fix/issue-766-inline-course-synopsis
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Validate helper
        run: echo "helper workflow is valid"
