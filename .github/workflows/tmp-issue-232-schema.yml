name: Patch issue 232 SAR release schema

on:
  push:
    branches: [agent/issue-232-official-sar-assembler]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/issue-232-official-sar-assembler
      - run: python .github/patch-issue-232-schema.py
      - name: Commit schema patch if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/backend/prisma/schema.prisma
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "qa: model immutable official SAR releases"
          git push origin HEAD:agent/issue-232-official-sar-assembler
